<style>[li-cloak_disabled="hide"] {
        display: none !important;
    }
    [x-cloak] {
        display: none !important;
    }
    /* Make text look crisper and more legible in all browsers */
    body {
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
    }
    /* Focus state style for keyboard navigation for the focusable elements */
    *[tabindex]:focus-visible,
    input[type="file"]:focus-visible,
    button:focus-visible {
        outline: 0.125rem solid var(--mappings--bg--background-secondary);
        outline-offset: 0.125rem;
    }
    button {
        padding: revert;
        border-radius: revert;
    }
    /* Get rid of top margin on first element in any rich text element */
    .w-richtext> :not(div):first-child,
    .w-richtext>div:first-child> :first-child {
        margin-top: 0 !important;
    }
    /* Get rid of bottom margin on last element in any rich text element */
    .w-richtext>:last-child,
    .w-richtext ol li:last-child,
    .w-richtext ul li:last-child {
        margin-bottom: 0 !important;
    }
    /* Apply "..." after 3 lines of text */
    .text-style-3lines {
        display: -webkit-box;
        overflow: hidden;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
    }
    /* Apply "..." after 2 lines of text */
    .text-style-2lines {
        display: -webkit-box;
        overflow: hidden;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
    }
    .swiper-button-disabled {
        opacity: 50% !important;
        pointer-events: none !important;
    }
    .styleguide_spacing {
        background: linear-gradient(to top, color-mix(in srgb, var(--mappings--bg--background-secondary) 20%, transparent), transparent);
    }
    .footer-menu_nav-link-wrap .icon-embed-xxsmall {
        display: none;
    }
    .footer-menu_nav-link-wrap:has(.footer-menu_nav-link[href*="https://"]) .icon-embed-xxsmall {
        display: flex;
    }
    ::-webkit-scrollbar {
        width: 1em;
    }
    ::-webkit-scrollbar-track {
        background: var(--mappings--bg--background-primary);
    }
    ::-webkit-scrollbar-thumb {
        background: var(--mappings--bg--background-secondary);
        transition: background-color 0.3s ease;
    }
    ::selection {
        color: var(--mappings--bg--background-primary);
        background-color: var(--mappings--bg--background-secondary);
    }
    .mini-cart_line-item_item:last-child {
        border: none;
    }
    .product-card_link::after {
        content: "";
        position: absolute;
        inset: 0;
        cursor: pointer;
        z-index: 20;
    }
    .product-card_link:focus-visible::after {
        outline-color: var(--mappings--border--border-alternate);
        outline-offset: .125rem;
        outline-width: .125rem;
        outline-style: solid;
    }
    .product-card_component:has(.product-card_link:hover) .product-card_overlay,
    .product-card_component:has(.product-card_link:focus-visible) .product-card_overlay {
        opacity: 0.5;
    }
    .product-card_component:has(.product-card_link:hover) .product-card_image.is-hover,
    .product-card_component:has(.product-card_link:focus-visible) .product-card_image.is-hover {
        opacity: 1.0;
    }
    input.mini-cart_line-item_quantity-adjust::after {
        content: "";
        position: absolute;
        inset: 0;
        cursor: pointer;
        pointer-events: none;
    }
    input.mini-cart_line-item_quantity-adjust:focus-visible::after,
    .product-card_link:focus-visible::after {
        outline-color: var(--mappings--border--border-alternate);
        outline-offset: .125rem;
        outline-width: .125rem;
        outline-style: solid;
    }
    /* .product-header_option:last-child {
        border: none;
    } */
    .dropdown-tabs_menu-wrap:not(:has(.w--open)) .dropdown-tabs_dropdown_component:first-child .dropdown-tabs_dropdown_list {
        display: block;
        opacity: 1.0;
    }
    .dropdown-tabs_menu-wrap:not(:has(.w--open)) .dropdown-tabs_dropdown_component:first-child .dropdown-tabs_dropdown_toggle {
        border-color: var(--mappings--border--border-secondary);
    }
    .collection-slider_item-component:hover .collection-slider_thumb-img,
    .dropdown-tabs_collections_item-link:hover .dropdown-tabs_collections_thumb {
        transform: scale(1.05);
    }
    .shapes-list_item-conponent:hover .shapes-list_item-thumb {
        transform: translateY(-0.25rem);
    }
    .w-nav-overlay {
        z-index: -1;
    }
    .shopify-policy__container {
        z-index: 2;
        background: var(--mappings--bg--background-primary);
        max-width: none;
        padding: 1rem 3rem 3rem 3rem;
        width: 100%;
    }
    @media screen and (max-width: 991px) {
        .shopify-policy__container {
            padding-left: 2rem;
            padding-right: 2rem;
        }
    }
    @media screen and (max-width: 479px) {
        .shopify-policy__container {
            padding-left: 1.25rem;
            padding-right: 1.25rem;
        }
    }
    .shopify-policy__container div {
        max-width: 40rem;
        margin: auto;
    }
    .shopify-policy__title h1 {
        font-size: 2rem;
        text-align: left;
    }
    .product-header_radio-list::-webkit-scrollbar {
        height: 0.0625rem;
    }
    .product-header_radio-list::-webkit-scrollbar-track {
        background-color: var(--mappings--border--border-primary);
    }
    .product-header_radio_component:has(.product-header_radio_image) .product-header_radio_button {
        aspect-ratio: 1/1;
    }
    html {
        scrollbar-gutter: stable;
    }
    body:has(.nav_menu[data-nav-menu-open]) .nav_icon-dropdown.is-cart {
        opacity: 0.0;
        pointer-events: none;
    }
    @media (min-aspect-ratio: 16/9) and (min-height: 1080px) {
        .container-large {
            max-width: min(calc(100vw * 0.75), var(--sizes--size--container--max-width));
        }
    }
    @media (min-aspect-ratio: 21/9) and (min-height: 1080px) {
        .container-large {
            max-width: min(calc(100vw * 0.8), var(--sizes--size--container--max-width));
        }
    }
    @media (min-height: 1440px) {
        .container-large {
            max-width: min(calc(var(--sizes--size--container--max-width) * (100vh / var(--sizes--size--container--base-height))), var(--sizes--size--container--max-width));
        }
    }
</style>
